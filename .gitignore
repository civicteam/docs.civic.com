# Dependencies
node_modules/

# npm lockfile — this repo uses pnpm (see package.json#packageManager).
# A stray package-lock.json would drift from pnpm-lock.yaml and confuse CI.
package-lock.json

# Docusaurus build + cache
.docusaurus/
build/

# Legacy Next/Mintlify outputs (kept so downgrade is clean if needed)
.next/
out/
dist/

# Environment files
.env
.env.local
.env.production

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Lychee link checker
lychee-report.md
lychee/
.lycheecache

.claude/
claude.md
changes.md
claude.md.backup
